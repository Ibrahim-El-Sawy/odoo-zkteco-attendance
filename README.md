# odoo-zkteco-attendance
REST API module for Odoo that receives and processes attendance logs from ZKTeco biometric devices.   This project provides a JSON endpoint, parses check-in/check-out times, prevents duplicate records,   and stores attendance data in a custom Odoo model linked to employees.
